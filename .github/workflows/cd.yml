name: CD

on:
  push:
    tags:
      - 'v*'

concurrency:
  group: release
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-binary:
    name: Build ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-x64
          - os: ubuntu-24.04-arm
            target: linux-arm64
          - os: macos-15-intel
            target: darwin-x64
          - os: macos-14
            target: darwin-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"

      - name: Build and package
        env:
          TARGET: ${{ matrix.target }}
        run: make package

      - uses: actions/upload-artifact@v7
        with:
          name: binary-${{ matrix.target }}
          path: |
            dist/timba-*.tar.gz
            dist/timba-*.sha256

  build-docker:
    name: Docker
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"

      - run: make docker

      - name: Get image ref
        id: image
        run: echo "ref=$(make docker-image)" >> "$GITHUB_OUTPUT"

      - name: Push to GitHub Container Registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          REPO="ghcr.io/${{ github.repository }}"
          docker tag ${{ steps.image.outputs.ref }} ${REPO}:${{ github.ref_name }}
          docker push ${REPO}:${{ github.ref_name }}
          docker tag ${{ steps.image.outputs.ref }} ${REPO}:latest
          docker push ${REPO}:latest

  release:
    name: Release
    needs: [build-binary, build-docker]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/download-artifact@v8
        with:
          pattern: binary-*
          merge-multiple: true
          path: artifacts

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ github.ref_name }}
        run: |
          gh release create "$VERSION" \
            --generate-notes \
            artifacts/timba-*.tar.gz \
            artifacts/timba-*.sha256 \
          || echo "Release already exists"

          for f in artifacts/timba-*; do
            gh release upload "$VERSION" "$f" --clobber 2>/dev/null || true
          done
